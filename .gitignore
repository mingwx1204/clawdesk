# ── Node 前端 ──────────────────────────────
node_modules/
# vite build 构建产物（tauri build 会重新生成并内嵌，无需入库）
dist/
*.local

# ── Rust / Tauri 后端 ──────────────────────
src-tauri/target/
src-tauri/gen/schemas/
src-tauri/gen/android/
src-tauri/gen/apple/

# ── IDE / 系统 ─────────────────────────────
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ── 日志 ───────────────────────────────────
*.log
npm-debug.log*

# ── 测试残留 / 临时文件 ────────────────────
tmp_ocr.*
*.bak
*.enc
test_edge_tts*.cjs
test_tts_*.mp3

# ── pnpm ──────────────────────────────────
pnpm-lock.yaml
pnpm-workspace.yaml

# ── Windows ────────────────────────────────
Thumbs.db
Desktop.ini
probe.pdb
probe.exe
