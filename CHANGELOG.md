# ClawDesk 版本记录

> 自 v0.5.0 起为 **Tauri 2 + Vue 3 + Rust** 全新架构（旧版 Capacitor/React 架构已弃用）。

## v0.7.0 (2026-08-12)

> 全模块深度优化：排查并修复 40+ 项已知问题（bug / 竞态 / 安全 / 性能），
> 升级打包与发布配置，全量测试通过（338 项）。

### 🐛 微信 Bot 修复（核心）
- **UI 自动发送 100% 失败修复**：`wechat_ui_send` 的 Ctrl+F 按键解析不支持字母键，现已支持 a~z 组合键
- **主动聊天生命周期修复**：扫码登录确认 / 手动「启动消息收发」后主动聊天循环立即启动（此前必须重启软件才生效）；「停止消息收发」现在会真正停止主动聊天
- **长轮询重连修复**：每轮重读 token（会话过期后新登录可接管）、-14 会话过期加指数退避（封顶 60s）、连续失败 5 次停止并提示重新扫码、重连不再无条件报"已连接"
- **消息去重提前**：重复投递的媒体不再反复下载写盘；无 seq 消息改用内容哈希兜底去重；get_updates 游标只接受单调前进回显（防历史重放导致重复 AI 回复）
- **媒体处理性能**：同步文件 IO 全部移出长轮询热路径（tokio::fs / spawn_blocking），大文件下载不再阻塞同批消息处理
- **AI 发言统计修正**：手动发送 / 自动回复不再被误计为"AI 主动发言"（存在感惩罚与静默机制只统计主动聊天）
- **登出清理**：换账号登录不再撞去重环 / 残留上次主动聊天记录
- **人设目标恢复修复**：proactive_target 为空时真正写回运行时目标并立即生效（此前注释与实现不符、永远空转）
- **热聊间隔尊重用户配置**：不再硬编码 2~6 分钟，取 max(配置最小值, 2~6 分钟)
- **聊天记录读取优化**：状态轮询 / 主动聊天 / 自动回复改为只读最近 200 条（此前每次全文件扫描）
- **CDN 上传 AES 密钥改真随机**（getrandom），filekey 加随机后缀防同毫秒碰撞
- **循环 panic 兜底**：getupdates 任务 panic 不再静默掉线，记录日志并提示
- **附件目录不再硬编码 D 盘**：改为数据目录下 `attachments\`；新增 7 天自动清理（限频 1 小时一次）

### 🐛 Agent 引擎修复
- **ZIP 解压炸弹防护**：解压总量限 256KB（防 2MB 高压缩比包 OOM）
- **SSE tool_calls index 上限**：恶意 index 不再扩张数组（防 OOM）
- **沙箱 junction/符号链接绕过修复**：路径校验统一走真实路径解析（canonicalize）
- **工具上限语义统一**：默认 15 轮与系统提示/前端一致；熔断按实际工具调用总数计算
- **中文工具名解码修复**：多字节 UTF-8 名称不再损坏为乱码
- **子代理白名单修正**：file_read/list_dir/search_text 等真实注册名（此前白名单 ID 错误导致子代理读不了文件）
- **工具白名单补全**：agent_subtask / web_search / knowledge_* / snapshot_* / wechat_ui_* 等全部对 Agent 可见
- **思考模式不再静默提权**：不再强制切 Yolo 跳过高危工具确认
- **runner 超时生效**：timeout_secs 真正包裹主循环；确认通道双 ID 统一（逐步确认不再"永远拒绝"）
- **terminal 管道死锁修复**：边执行边读输出（不再等退出导致 64KB 阻塞），超时杀进程树
- **工具结果截断**：单条结果限 32KB 回填（防击穿上下文窗口）
- **上下文压缩可取消**：压缩等待支持取消令牌
- **压缩后清理悬空 tool 配对**：修复压缩导致 HTTP 400
- **快照索引加锁**：并行工具调用不再丢快照记录；备份文件名加随机后缀防同毫秒覆盖
- **搜索/知识库扫描上限**：文件数/深度限制防大目录卡死
- **阻塞调用移出 async**：Off/PlanOnly 直通路径包 spawn_blocking；RUSTLS 提示只打印一次

### 🔒 安全
- **opencode 回切 Key 不再明文落盘**：迁入 DPAPI 加密 keys.enc，旧 settings.json 明文自动迁移清除
- **CSP 开启**：最小化内容安全策略（替代完全关闭），v-html 渲染有了兜底防线

### 💻 前端
- **乱码修复**：错误提示 "[閿欒]" → "[错误]"（GBK/UTF-8 混编损坏）
- **图片查看器键盘失效修复**：Esc / ← / → 快捷键重新注册
- **运行中禁止切换会话**：防止流式输出写入错误会话
- **删除假功能**：假压缩按钮移除（后端已有自动压缩）；自检/导出接入真实后端
- **工具调用按实例去重**：同一工具多次调用不再互相覆盖
- **权限模式启动同步**：重启后顶栏显示与后端一致
- **"第 N 轮"显示真实轮次**：不再恒为 1
- **字号设置生效**：12~22px 实际应用到界面
- **微信自动回复并发锁**：每槽位串行回复（防会话记忆丢失/回复乱序）
- **QR 轮询修复**：递归 setTimeout 替代 5s interval（不再堆积并发请求）；二维码过期明确提示
- **槽位切换残留修复**：清空最近消息 / 重置语音设置编辑态（防把 A 槽位设置写进 B）
- **VmPanel 关闭停止画面流**：不再后台持续截图耗 CPU
- **暗色主题补全**：标题栏/弹窗/列表项对比度修复（此前部分文字不可读）
- **性能**：Markdown 渲染缓存、watch 去 deep、附件并行上传、粘贴支持任意文件
- **删除 7 个死代码组件**；VmPanel 模板残留重复按钮/多余闭合标签修复（构建报错根因）

### 📦 构建与发布
- **bundle 只保留 NSIS**（移除依赖 WiX 的 MSI，构建更快更稳）；安装器头部图改 PNG
- **release profile 加 codegen-units=1**（配合 LTO 减体积提速）
- **测试修复**：7 个测试改为数据目录隔离（此前并行污染真实 D:\ClawDeskData）；tool_log 测试断言路径修正；全量 338 项测试通过

---

## v0.6.0 (2026-08-11)

> 本次版本基于对开源项目 **AstrBot**（AstrBotDevs/AstrBot）的源码级研究，移植其微信官方协议实现中的先进能力，并对架构做了清理与升级。详见 `docs/AstrBot对比研究报告.md`。

### ✨ 微信 Bot：语音听懂
- **语音云端转写**：语音消息直接提取腾讯云端转写文本（`voice_item.text`）拼入消息文本，AI 无需 ASR 即可听懂语音内容（字段缺失时自动降级为原行为）
- 语音转写文本单独透出（`voiceTranscript`），前端可标记"这是语音"

### ✨ 微信 Bot：引用消息
- **引用消息解析（ref_msg）**：用户引用文本/图片/语音提问时，被引用内容解析进上下文（`[用户引用了消息：…]`），被引用媒体自动下载供 AI 读取——"引用图片问这是什么"场景完整可用

### ✨ 微信 Bot：typing 状态机
- **"对方正在输入"保活**：AI 生成期间每 10s 发送 typing（微信端持续显示输入中），回复完成自动结束；成功/失败均清理，不残留输入态
- 新命令 `wechat_typing`（active/stop），前端自动回复链路自动接入

### ✨ 微信 Bot：断点续拉
- **get_updates 游标（sync_buf）+ context_token 持久化**：重启后从断点续拉消息（不重收不丢失），且无需等新消息即可回复旧会话（context_token 已恢复）
- 旧版账号文件无缝兼容（serde default 字段）

### ✨ 微信 Bot：能力声明
- `wechat_bot_status` 输出 `capabilities` 能力矩阵（文本/图片/语音转写/引用/typing/主动 均支持；群聊/语音发送为协议天花板如实声明），前端类型同步

### ✨ 微信 Bot：主动聊天拟人化
- **人设兼容**：生活状态作为"世界日常节奏"背景注入而非硬性规定，AI 结合自定义人设自然演绎（猫→晒太阳/追毛线、机器人→待机/充电、修仙者→闭关/炼丹），prompt 明确"你的一切言行与状态都必须符合你的人设"——任何自定义人设都不违和
- **AI 一生记忆（连续世界线）**：生活事件落盘到 `D:\ClawDeskData\living\{YYYY-MM}.jsonl`（按月分文件），重启后恢复；出生档案（首次运行建档，AI 知道自己"出生于 X，已生活 N 天"）；今日轨迹（"你今天的生活：08:12 🥛 在吃早饭…"）+ 近期记忆（最近 2 天时间线）注入主动聊天与自动回复 prompt——AI 记得自己昨天/前天在做什么，跨天连续
- **AI 生活状态（世界线）**：AI 有自己的日常作息——按时段生活（7 点起床/8 点早饭/上午搬砖/中午干饭/傍晚晚饭/晚上打游戏刷视频/21 点后洗漱躺平/23 点睡/凌晨勿扰），时间与真实时钟同步；同一小时内状态稳定不漂移；40 分钟内跨时段有"刚才在干嘛"衔接（如"你刚吃完晚饭，现在躺着刷手机"）。用户问"你干嘛呢"AI 能如实回答；主动聊天和自动回复 prompt 均注入
- **连续对话（热聊）模式**：用户 30 分钟内回过消息 → 自动进入热聊（2~6 分钟短间隔续话，像聊开了的节奏）；长时间没人回自然冷却回普通模式。热聊中静默守卫放宽（允许短间隔接话），prompt 切到"续话式"（顺着对方刚说的内容接一句，非重新问候），冷场自动切回"开场式"
- **时段加权间隔**：晚高峰（18~22 点）更活跃（间隔缩短 2.5×）、早高峰（8~9 点）1.5×、午间（12~13 点）1.2×，模拟真人发消息的时段偏好（保底 min/2 分钟防连发）
- **拟人 prompt v2**：不再"你是机器人/决定要不要发任务"，改为真人身份思考"此刻有没有由头"；few-shot 给出早/午/晚自然开场口吻（学语气非照抄）
- **上次发送时间差注入**：prompt 告知"你上次主动发消息是 X 分钟/小时前"，避免重复问候同一时段
- **面板显示 AI 生活状态**：微信面板新增"AI 生活状态"行（如"🎮 在放松（在打游戏，排位连跪中）"），5 秒随状态刷新

### ✨ 微信 Bot：消息体验
- **消息去重**：getupdates 重复投递同一消息时跳过（保留最近 100 条 msg_id），避免 AI 重复回复
- **消息类型细分**：语音转写消息标记为 `voice`，微信面板显示 🔊（图片 🖼️ / 文件 📎 同步）
- **媒体失败可见**：媒体下载/解密失败不再静默，日志明确提示（方便排查）

### ✨ 定时任务
- **微信推送槽位可选**（0=微信1 … 9=微信10）：任务结果可推送到任意已登录微信，不再硬编码微信1；旧任务默认槽位 0 兼容

### ⚙️ Agent 引擎
- **上下文 token 预算截取**：历史从"固定 12 条"改为 32K token 预算（按内容长度粗估），微信短聊保留更多轮次、长工具输出不撑爆上下文

### 🧹 架构清理
- **死代码清理**：删除 80 个未挂载的旧 React 组件（14 个顶层 .tsx + settings/ + ui/ 组件库），`vue-tsc` 全量检查通过
- **微信自动回复下沉**：`autoReplyWechat` 从 App.vue 抽离为独立 composable（`src/composables/useWechat.ts`）
- **Edge TTS 修复**：适配 reqwest-websocket 0.6.0 API（Upgrade trait / into_websocket / try_next），恢复编译

### 🐛 修复
- 长轮询循环落盘优化：游标/context_token 变更才写盘（DPAPI 加密开销可控）

---

## v0.5.2 (2026-08-10)

### ✨ 微信 Bot
- 主动聊天**拟人化**：静默机制（AI 发过、用户没回 → 不发）、AI 自主决策（该不该发交给 AI 判断）、空闲退避（间隔指数拉长）、存在感惩罚（AI 占比过高闭嘴）、深夜不打扰（23:00~08:00）
- 主动聊天设置/目标用户/上次主动时间**持久化**，重启自动恢复
- 目标用户从聊天记录自动恢复（重启后无需等新消息）

### 🛠️ 附件与文件
- **PDF 文本提取**：`file_read` 自动提取 PDF 文本（纯 Rust 解析）
- **ZIP 自动解压**：≤2MB 压缩包自动解压查看；超过 2MB 明确告知
- **图片格式按文件头识别**：JPEG/PNG/GIF/WEBP/BMP 真实格式，不再"扩展名 .png 实际是 JPEG"

### ⚙️ 设置
- **工具循环上限可配置**（推荐 5~15 / 范围 1~30），防 AI 死循环
- 设置面板新增对应输入框（Agent 参数页）

### 🎭 对话体验
- **去 AI 味**：默认 5~40 字短句口语化回复；**只有用户明确要求字数才写长文**
- 后端兜底截断：超长回复自动精简

### 🐛 修复
- 中文消息 UTF-8 边界崩溃（曾导致长轮询断连、手机端"暂无法连接"）
- getupdates 长轮询断连自动重连 + 详细错误日志
- 人设写盘失败（只读属性自动清除 + 写盘失败报错不静默）
- 人设编辑被 5 秒轮询覆盖（personaDirty 编辑保护）
- 视觉模型配置不匹配（opencode 网关需用 mimo-v2.5 等支持图片输入的模型）

### 📦 发布
- 独立可执行文件（前端资源内嵌，无需开发服务器）
- 桌面快捷方式指向 release 版，双击即用

---

## v0.4.1 (2026-08-09)（旧架构存档）
- 旧版 Capacitor/React 架构的最终版本，已被 v0.5.0 全新架构取代
