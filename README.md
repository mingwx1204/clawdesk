# ClawDesk — AI 桌面助手

> **当前版本：v0.8.0**（2026-08-14）

基于 **Tauri 2 + Vue 3 + Rust** 的 Windows 桌面智能体：集成 **微信 Bot**、**多模型路由**（主对话 / 识图 / 生图）、**Agent 工具链**、**定时任务**、**本地记忆** 于一体，本地优先，数据全部保存在本机。

---

## ✨ 功能总览

| 模块 | 说明 |
|---|---|
| 💬 主对话 | AI 聊天、流式输出、思考链、图片附件、多会话管理 |
| 📱 微信 Bot | **单微信账号**，扫码登录、自动回复、主动聊天、人设、记忆共享 |
| 🤖 智能 Agent | ReAct 工具循环、规划、逐步确认、断点续跑 |
| 🛠️ 工具系统 | 文件读写、识图、OCR、生图、浏览器、知识库、MCP 插件 |
| ⏰ 定时任务 | 周期性自动执行任务（含给微信发消息） |
| 🎨 图像生成 | 文生图（Flux 等，多厂商 API） |
| 🔍 OCR 识别 | 本地图片文字识别 |
| 🔌 MCP 插件 | 注册外部 MCP server，扩展工具能力 |
| ⚙️ 设置中心 | 模型 API、Agent 参数、外观、安全、技能、自进化 |
| 🧠 记忆系统 | 会话持久化（SQLite）、快照回滚、知识库 RAG |

---

## 📦 安装与启动

### 方式一：正式版（推荐）
直接运行 `clawdesk.exe`（安装版位于 `C:\ClawDesk\`），双击桌面快捷方式启动。

### 方式二：开发模式
```powershell
# 需要 Node.js 18+ 与 Rust 工具链
cd ClawDesk
npm install
.\start-dev.ps1        # 一键启动（自动编译 Rust + 前端，日志写到 D:\clawdesk_dev.log）
```

### 构建正式版
```powershell
npm run tauri build    # 生成独立可执行文件（前端资源内嵌，无需服务器）
```

---

## 💬 主对话（AI 聊天）

- **流式回复**：AI 回答逐字显示，支持思考链折叠查看
- **Markdown 渲染**：代码块、表格、列表、公式
- **图片附件**：拖拽/粘贴图片 → AI 调用视觉模型识别
- **文件附件**：任意文件（文本 / PDF / ZIP / 图片等）→ AI 自动读取内容
- **会话管理**：新建 / 重命名 / 删除 / **Fork 分支** / **断点续跑** / 导出 Markdown / 关键词搜索历史
- **消息操作**：复制 / 朗读（TTS）/ 重新生成 / 编辑重发
- **多模型路由**：主对话、识图、生图各走独立模型，互不干扰

---

## 📱 微信 Bot（核心特色）

### 接入
1. 打开微信面板（顶部工具栏 🐧 按钮）
2. 点击「获取二维码」→ 手机微信扫码
3. 扫码确认后自动登录，**软件重启后自动续连**（凭据 DPAPI 加密保存）

### 账号
- **单微信账号**（历史多槽位已于 v0.8 收敛）：登录、人设、聊天记录、AI 会话记忆均独立存放

### 自动回复
- 收到消息 → AI 自动回复（默认开启，面板可关）
- **人设**：该微信的独立人设（存 `persona.md`），AI 按人设说话
- **记忆共享**：自动回复与主动聊天共用同一份会话记忆，不"人格分裂"

### 主动聊天（拟人化）
- 随机间隔触发（**1~180 分钟可调**），AI 主动找用户聊天
- **拟人机制**（参考 MaiBot 设计）：
  - 🤫 **静默机制**：AI 发过消息、用户没回 → 保持安静，等用户先开口
  - 🧠 **AI 自主决策**：生成前 AI 自评"现在值得打扰吗"，不值得就不发
  - ⏳ **空闲退避**：用户持续不回 → 间隔指数拉长（封顶 60 分钟），回复后立即恢复
  - 🚫 **存在感惩罚**：AI 发言占比过高时自动闭嘴
  - 🌙 **深夜不打扰**：23:00~08:00 不主动
- **像真人说话**：默认 5~40 字短句、口语化；**只有用户明确要求字数才写长文**

### 媒体处理
| 类型 | 处理方式 |
|---|---|
| 图片 | 按文件头识别真实格式（JPEG/PNG/GIF/WEBP/BMP）→ 视觉模型识图 |
| PDF | `file_read` 自动提取文本（纯 Rust 解析） |
| ZIP 压缩包 | **≤2MB 自动解压**查看内容；超过 2MB 明确告知不支持 |
| 语音/视频 | 下载保存到本地附件目录 |
| 文本文件 | 直接读取内容（限制 1MB） |

- 聊天记录保存在 `D:\ClawDeskData\wechat\slot{N}\history.jsonl`

---

## 💬 独立微信（AI 用"真人微信号"回复）

不想用 Bot？AI 可以直接操作一个**真实微信**，两种方式内置：

| 方式 | 说明 | 前置条件 |
|---|---|---|
| 🖥️ **虚拟机内置微信**（推荐） | VirtualBox 跑 Win11 + 微信，画面内嵌、AI 可操作；与主系统完全隔离 | 安装 VirtualBox，已有 `AI-WeChat` 虚拟机 |
| 📄 **本机多开微信** | 直接操作本机多开的微信窗口（无需虚拟机） | 已多开一个微信并登录，窗口勿最小化 |

### 启用 AI 自动回复（以虚拟机方式为例）

1. 顶部工具栏点击 🖥️ 按钮打开「虚拟机内置微信」面板
2. 连接虚拟机（`连接屏幕` → VNC 自动连）→ 画面内嵌显示
3. 在面板里设**白名单**（AI 只能给名单里的人发消息）
4. 点击「🤖 开启 AI 托管」→ 后台每 10s 监视屏幕，有新消息自动截图给 AI 处理回复
5. 可选「💬 开启主动聊天」：AI 定时（随机 5~180 分钟）主动找白名单对象聊天
6. 安全：「🔒 只读模式」下 AI 只能看屏幕不能操作

### 回复通道切换（Bot ↔ 独立微信）

顶部工具栏的通道按钮（🤖 Bot 回复 / 🖥️ 独立微信回复 / ⏸ 回复已关）**点击循环切换**，
二选一互不冲突；面板里的旧开关仍可单独控制，状态自动同步。

---

## 🤖 智能 Agent

- **ReAct 工具循环**：AI 自主规划 → 调用工具 → 观察结果 → 继续，直到完成
- **权限模式**：`关闭`（直通模型）/ `计划只读` / `逐步确认`（高危操作先弹窗）/ `YOLO 全自动`
- **工具循环上限**：单轮最多调用工具次数（设置可调，**推荐 5~15，范围 1~30**），防死循环
- **断点续跑**：任务中断后从断点恢复
- **进度事件**：实时显示当前轮次、思考、工具调用

### 内置工具（部分）
文件读写 / 目录列表 / 图片识别（`analyze_image`）/ OCR / 生成图片 / 浏览器自动化 / 知识库检索 / 记忆搜索 / MCP 工具 / 终端命令（沙箱内）

---

## ⏰ 定时任务

- 创建周期性任务：自定义指令 + 执行时间
- 支持给指定微信发消息（结合人设）
- 任务持久化，重启不丢失

---

## 🎨 图像生成

- 输入 prompt + 尺寸 → 生成图片（Flux 等模型）
- 独立 API 配置（SiliconFlow 等，与主对话分离）

---

## 🔍 OCR 识别

- 上传图片 → 本地 OCR 提取文字
- 依赖系统 Tesseract 引擎

---

## 🔌 MCP 插件

- 注册外部 MCP server（stdio：command + args）
- 连接后自动注册其工具，扩展 AI 能力
- 配置持久化，重启自动恢复

---

## 🧠 记忆与安全

- **会话持久化**：SQLite 存储，重启不丢
- **上下文压缩**：超阈值自动摘要压缩（阈值可调）
- **知识库 RAG**：本地文本向量检索
- **快照回滚**：文件修改前自动备份，可对比/回滚
- **敏感文件保护**：拦截 .env / 密钥 / 凭据等敏感路径
- **沙箱目录**：只允许 AI 访问授权目录

---

## ⚙️ 设置中心

| 标签页 | 功能 |
|---|---|
| 模型 API | 主模型 / 视觉模型 / 绘图 API 的 Key、端点、余额查询 |
| Agent 参数 | 权限模式、最大迭代轮数、压缩阈值、**工具循环上限** |
| MCP 管理 | 注册 / 移除 MCP 服务器 |
| 外观汉化 | 中文模式、字号、深色模式、界面透明度、时区 |
| 快照与安全 | 快照开关、高危确认、敏感文件保护、开机自启、日志路径 |
| 技能管理 | 技能列表、启用/禁用、重新扫描 |
| 系统运维 | 沙箱目录、日志查看、自检、导出数据 |
| 自进化 | AI 自动分析失败任务、生成改进技能 |

---

## 🗂️ 数据目录（全部本地）

```
D:\ClawDeskData\
├── settings.json          # 应用设置
├── keys.enc               # API Key（DPAPI 加密）
├── sessions.db            # AI 会话记忆（SQLite）
├── knowledge.db           # 知识库（SQLite）
├── logs\                  # 调试/审计日志
├── snapshots\             # 文件快照备份
├── exports\               # 导出数据
└── wechat\
    └── slot{N}\           # 每个微信：账号 / 人设 / 聊天记录 / 主动聊天设置
```

---

## 🛠️ 技术栈

- **前端**：Vue 3 + TypeScript + Vite
- **后端**：Rust + Tauri 2
- **微信接入**：腾讯 iLink Bot API（官方扫码登录，纯 HTTP）
- **存储**：SQLite + JSON 文件
- **模型**：OpenAI 兼容协议（DeepSeek / OpenCode Go / 智谱 / SiliconFlow 等）

---

## 📄 文档

- [开发规格说明书](docs/DEV_SPEC.md)

---

## 📝 更新日志

### v0.7.0（2026-08-12）
- **微信 Bot**：UI 自动发送修复、主动聊天生命周期修复（扫码/启动即生效、停止即停）、长轮询断点续拉与退避重连、媒体去重提前、附件目录去硬编码+自动清理、CDN 密钥改真随机
- **Agent 引擎**：ZIP 解压炸弹防护、沙箱 junction 绕过修复、工具上限统一 15、子代理白名单修正、terminal 管道死锁修复、超时生效、确认通道统一、压缩可取消
- **安全**：opencode Key 不再明文落盘（DPAPI）、CSP 开启
- **前端**：乱码修复、图片查看器快捷键、运行中禁切会话、去假功能（压缩/自检/导出接真实后端）、暗色主题对比度、性能优化（markdown 缓存/并发上传）
- **构建**：NSIS-only 打包、codegen-units=1、全量 338 项测试通过

### v0.5.2（2026-08-10）
- **微信 Bot**：主动聊天拟人化（静默机制 / AI 自主决策 / 空闲退避 / 存在感惩罚）
- **微信 Bot**：目标用户与设置持久化，重启自动恢复
- **修复**：中文消息 UTF-8 边界崩溃（曾导致长轮询断连）
- **修复**：长轮询断连自动重连 + 详细日志
- **修复**：人设写盘失败（只读属性自动清除）
- **视觉模型**：切换为支持图片输入的模型，识图正常
- **附件**：PDF 文本提取、ZIP（≤2MB）自动解压、图片格式按文件头识别
- **工具循环上限**：设置可调（推荐 5~15 / 范围 1~30）
- **去 AI 味**：默认短句口语化回复，用户要求字数才写长文
- **发布**：独立可执行文件（前端内嵌，无需开发服务器）

---

## ⚠️ 注意

- API Key 仅保存在本机（DPAPI 加密），请勿将 `settings.json` / `keys.enc` 提交到公共仓库
- 微信 Bot 需保持软件运行才能接收消息（关闭窗口 = 后台常驻托盘）
