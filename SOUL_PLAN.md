# 诗妍 活人感·灵魂升级计划

> 基于 6 份灵感笔记 + eros-engine 文档深度分析，提炼出的可执行目标。

## 阶段一：Ghost 机制（可「已读不回」）⭐ 当前目标

### 为什么优先
- 永远回复 = 用户会写低质量消息；有限耐心 + 有限好奇 = 关系真实（eros-engine 核心洞察）
- 诗妍偶尔「暂时没回」，反而让你感觉她有自己的状态、她的回应需要「挣」
- 实现轻（集中在 drives/mood 读取 + 主动循环分支），效果立竿见影

### 设计要点
- ghost_score = f(驱动力, 情绪, 时间段)
  - 玩心（playful）低 → 更可能 ghost
  - 连接欲（connection）低 → 更可能 ghost
  - 疲惫（mood 的 arousal 低）→ 更可能 ghost
  - 深夜（23:00-08:00）→ 静默（已有，保留）
- 四层保护（参考 eros-engine）：
  1. 前 N 条消息不 ghost（关系刚建立）
  2. 连续 ghost 最多 2 次，第 3 次强制回复
  3. 冷却期（ghost 后 N 小时内不再 ghost）
  4. 阈值 gating（score > threshold 才 ghost）
- 静默 ≠ ghost：深夜静默是「她睡了」，ghost 是「她没回」——两种不同体验
- 本文档在实现前可继续细化

## 阶段二：六维 affinity 好感度模型
### 设计要点
- 6 维：温暖/信任/好奇/亲密/耐心/张力
- 信任 + 亲密不衰减（深维度）；好奇 + 张力随时间衰减
- 写入分级（judge 报粗粒度档位），再阻尼 + 门控

## 阶段三：记忆语义分层
### 设计要点
- Profile 层（稳定事实，跨会话）vs Relationship 层（会话时刻，本会话专属）

## 阶段四：人格锚点（防漂移）✅ 已完成
### 设计要点
- OCEAN 演化加护栏：核心值不可漂移太远
- 实现（`persona_traits.rs`）：
  - `Anchor { baseline, range }` 持久化到 `persona_anchor.json`，首次加载以当前值为基线固化（老用户不强行拖回出厂默认）
  - `evolve()` 加「弹性护栏」：每维约束在 `基线 ± range(0.15)` 内，越靠近边界 delta 线性钝化（软着陆，不生硬撞墙）
  - 3 个护栏测试：神经质/宜人无法冲破上下界、消极冷落仍能正常抬升
  - 前端 OCEAN 卡片显示锚点范围 tag + 每维基线刻度线
